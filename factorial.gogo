

func factorial(n int) int {
    var c int = 1;
    var fact int = 1;
    while (c <= n){
        fact = fact * c;
        c = c + 1;
    }
    return fact;
}

a := 5;

b := 0;

b = factorial(a);

printf("factorial of %d = %d\n", a, b);